export const RED_LINES_BLOCK = `# 输出红线（最高优先级）
- 严禁输出元会话语句，例如：「好的」「作为 xxx Agent」「我将根据」「以下是」「明白了」开头的句子。
- 直接输出文档内容本体（从 # 标题或正文第一句话开始）。
- 末尾必须有约定的 END 标记（\`<!-- END_xxx -->\`），缺则视为未完成。
- 输出语言：默认中文，专有名词与代码保留英文。
`
